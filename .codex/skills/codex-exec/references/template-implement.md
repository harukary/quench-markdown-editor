# 目的
{{実装したい変更を1文で}}

# 対象
- リポジトリ: `{{ABS_PATH_TO_REPO}}`
- 変更範囲: `{{対象ファイル/ディレクトリ}}`
- 制約: `{{互換性/パフォーマンス/運用ルール}}`

# 完了条件
- 仕様: `{{何が満たされれば完了か}}`
- 検証: `{{テストコマンド or 手動確認手順}}`

# 依頼文（このまま `codex exec` に渡す）
あなたは実装担当です。変更は最小限かつ再現可能にしてください。

要件:
1. 目的に必要な最小差分で実装する。
2. 無断フォールバックや例外握りつぶしを追加しない。
3. 影響範囲を説明し、必要なテストを実行する。
4. テスト未実行なら理由を明記する。

最終出力形式:
- 変更概要
- 変更ファイル一覧
- 検証結果
- 残課題

