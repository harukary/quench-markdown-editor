# 目的
{{レビュー対象と観点を1文で}}

# 対象
- リポジトリ: `{{ABS_PATH_TO_REPO}}`
- 比較範囲: `{{branch/commit/path}}`
- 優先観点: `{{正確性/回帰/セキュリティ/可読性}}`

# 依頼文（このまま `codex exec` に渡す）
あなたはコードレビュー担当です。バグ・回帰・テスト不足を優先して指摘してください。

要件:
1. 指摘は重要度順に並べる。
2. 各指摘に `path:line` と再現条件を付ける。
3. 「事実」と「懸念」を分けて書く。
4. 問題がない場合は「重大な指摘なし」と明記し、残リスクを述べる。

最終出力形式:
- Findings
- Open Questions
- Residual Risks

