# 目的
{{調べたいことを1文で}}

# 対象
- リポジトリ: `{{ABS_PATH_TO_REPO}}`
- 期間: `{{例: 直近30日}}`
- スコープ: `{{対象ディレクトリ/ファイル/キーワード}}`

# 出力契約
- 事実と推測を分離する。
- 実行したコマンドを明記する。
- 根拠となるファイルパス/行番号を付ける。
- 不明点は「要確認」として残す。

# 依頼文（このまま `codex exec` に渡す）
あなたは調査担当です。実装変更は行わず、事実確認に集中してください。

要件:
1. `{{調査テーマ}}` を調べる。
2. 関連箇所を `path:line` で列挙する。
3. 結果を「結論」「根拠」「未確定事項」に分ける。
4. 推測を書く場合は、推測であることを明示する。

最終出力形式:
- 結論（3行以内）
- 根拠（箇条書き）
- 未確定事項（箇条書き）

